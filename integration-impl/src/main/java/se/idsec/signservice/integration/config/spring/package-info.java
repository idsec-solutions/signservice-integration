/**
 * Spring Framework is optional when using the SignService Integration library. However, for some configuration cases,
 * users of Spring may use some (well-documented) features. For this we need access to the Spring application context.
 */
package se.idsec.signservice.integration.config.spring;